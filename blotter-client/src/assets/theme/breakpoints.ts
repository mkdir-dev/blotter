import { BreakpointsOptions } from '@mui/material/styles';

export const breakpoints: BreakpointsOptions = {
  values: {
    xs: 480, // extra-small (default: 0px)
    sm: 768, // small (default: 600px)
    md: 992, // medium (default: 900px)
    lg: 1200, // large (default: 1200px)
    xl: 1600, // extra-large (default: 1536px)
  },
};
